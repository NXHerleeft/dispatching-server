class ConnectionStatus {
    /**
     * If the connection is considered stable or not
     * @returns {boolean}
     */
    isStable(): boolean {
        return false;
    }

    toArray(): void {
    }
}